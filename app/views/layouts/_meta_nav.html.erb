<nav class="navbar navbar-inverse navbar-meta navbar-static-top">
  <div class="container">
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to setting(:title), root_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li class="dropdown">
          <%= link_to '#', class: 'dropdown-toggle', data: { toggle: 'dropdown' }, role: 'button' do %>
            <%= fa('user') %> <%= fa('caret-down') %>
          <% end %>
          <ul class="dropdown-menu">
            <li><%= link_to t(:usercp), '#' %></li>
            <li role="separator" class="divider"></li>
            <% if current_user.is_admin? %>
              <li><%= link_to t(:admin), admin_root_path %></li>
            <% end %>
            <li><%= link_to t(:logout), logout_path, method: :delete %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to t(:login), login_path %></li>
        <li><%= link_to t(:register), register_path %></li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>
