<div class="projects listing content">
	<h2 class="page_title"><%=t :projects %></h2>
	<ul id="projects">
	<% @projects.each do |p| %>
		<li class="project">
			<h3><%=link_to p.name, p %></h3>
		</li>
	<% end %>
	</ul>
</div>
